<%= link_to "Back", verbs_path, :class => "btn btn-primary pull-right" %>
<% if is_admin %>
	<%= link_to "Delete", verb_path(verb), :method => :delete, :class => "btn btn-danger pull-right", data: { confirm: "Are you sure?" } %>
	<%= link_to "Edit", edit_verb_path(verb), :class => "btn btn-success pull-right" %>
<% end %>